{
  "scripts": {
    "build": "npx parcel build manifest.json ",
    "start": "npx parcel watch manifest.json --host localhost "
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.11.0",
    "@types/chrome": "^0.0.256",
    "chrome-types": "^0.1.248",
    "parcel": "^2.11.0"
  },
  "dependencies": {
    "rxjs": "^7.8.1"
  }
}
